http://172.16.51.131:8000/

module 1
activity 1

ec2>
instance (already created)>   			we have to login, authenticate and perform actions
copy pblic ip addr>






aws academy>
details>
show>
download pem>
$ cd Downloads
$ sudo chmod 600 labsuser.pem
$ sudo ssh -i labsuser.pem ec2-user@44.204.170.217
$ cat /etc/os-release
$ python3 --version                 to check if ti is already installed
$ sudo yum install python3-pip
$ pip3 --version
$ sudo pip3 install awscli
	//we have not authenticated, so we have to do the below steps
$ aws ec2 describe-instances
	//we need to specify region
$ aws configure
AWS Access Key ID [None]: sss
AWS Secret Access Key [None]: ss
Default region name [None]: us-east-1a
Default output format [None]: json






aws academy>
details>
aws cli>
show> 
copy the text>

$ sudo nano ~/.aws/credentials
$ aws ec2 describe-instances

$ aws iam list-users
	//we have entered the wrong region name, so we are entering it again
$ aws configure
AWS Access Key ID [****************VPF2]: 
AWS Secret Access Key [****************2IV0]: 
Default region name [us-east-1a]: us-east-1
Default output format [json]: json
$ aws ec2 describe-instances





 
iam>
users>
awsstudent(already created)>
policy name>
lab_policy(already created)>
json>		to check the permission


	\\we have to transfer this json code to our terminal
$ aws iam list-policies --query "Policies[?PolicyName=='lab_policy'].Arn" --output text
	\\we are provided with the arn policy
	arn:aws:iam::199043984744:policy/lab_policy
$ aws iam get-policy --policy-arn arn:aws:iam::199043984744:policy/lab_policy --query 'Policy.DefaultVersionId' --output text
v1 
$ aws iam get-policy-version --policy-arn arn:aws:iam::199043984744:policy/lab_policy --version-id v1 --query 'PolicyVersion.Document' --output json > lab_policy.json
$ ls
$ cat lab_policy.json
	\\we are provided with the json code

